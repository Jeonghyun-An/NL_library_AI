14 한국도서관·정보학회지 (제35권 제3호)

1. 고빈도 색인어 추출기

대량의 변환된 텍스트데이터베이스를 입력으로 받아 간단한 형태소 분석을 통해 명사를 추출한다. 추출된 명사로부터 색인어 후보가 될 수 있는 모든 단일 명사와 복합 명사를 추출해 낸다. 추출한 후보 중 불용어 사전을 통해 제거된다. 기존의 색인어 추출 시스템은 문서 내에 출현한 모든 단일 명사를 합성 명사 구성 조건에 의해 복합 명사 색인어로 추출함으로 너무 많은 색인어가 추출되어 시스템의 정확률을 저하시키는 원인이 된다. 그러므로 본 연구에서는 추출된 후보들 중에서 출현 빈도가 높은 상위 30%~40%에 대해서만 명사 합성을 수행한다. 고빈도어 추출을 위한 빈도 계산은 식(6)에 의해 이루어지고 고빈도어 추출 알고리즘은 <그림 9>와 같다.

(6) Termfreq_ij = (문서 i에서 명사 j의 출현횟수 / 문서 i에서 추출된 전체 명사개수) * 100

[알고리즘 1] High_freq_extraction

| Input | 형태소 해석결과 A₁, ..., Aₙ | Output | 고빈도어 추출결과 H₁, ..., Hₙ |
| --- | --- | --- | --- |
| Input | 원하는 빈도 P₁, ..., Pₙ | Output | 고빈도어 추출결과 H₁, ..., Hₙ |
| High_freq_extraction_mehod |  | Q_Sort(H); |  |
| for i = 0 to n do |  | for i = P₁ to Pₙ do |  |
| Hᵢ.count = Aᵢ.count; |  | j = 0, bound = 0; |  |
| for j = i+1 to n do |  | do { |  |
| if (Aᵢ.pumsa == 'N') |  | bound = 고빈도 bound(H); |  |
| if (Aᵢ.data == Aⱼ.data) |  | //빈도의 누적값을 보여준다. |  |
| Hᵢ.count = Aᵢ.count + Hᵢ.count; |  | j++; |  |
| end if |  | } while (bound < Pi); |  |
| end if |  | if((Pi)-bound>bound 다음값(Pi)) |  |
| enddo |  | 고빈도_출력(H, bound); |  |
| enddo |  | else |  |
| enddo |  | 고빈도_출력(H, bound); |  |
| enddo |  | end if |  |
| enddo |  | enddo |  |

<그림 9> 고빈도어 추출 알고리즘

2. 합성명사 생성기

명사를 합성하는 이유는 광의의 단어가 색인됨으로써 검색시스템의 정확성을 떨어뜨리고 사용자의 탐색 시간을 늘린다. 본 연구의 합성명사 생성기는 기존 합성규칙(11)과 추가 제안한 규칙(12) 9가지에 따라 고빈도어를 대상으로 합성명사를 생성한다. <그림 10>은 합성명사 생성기의 흐름도이다.

11) 김민정, 한글 특성에 고려한 자동 색인기법(석사학위논문, 부산대학교 대학원 전자계산학과, 1993), p.35.
12) 김미진, 박미성, 최재혁, 이상조, 전계논문, pp.1125-1126.

- 284 -